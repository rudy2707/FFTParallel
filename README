To run the seq algo:
    make seq

To run the 1 Dimension parallel algo:
    make one

To run the Parallel algo:
    make run

To test the 1 Dimension parallel algo:
    make test
    octave --no-gui
    check
